<div class="container">
    <div class="new-form-content mt-3">
        <%= simple_form_for [ @cocktail,  @dose] do |f| %>
        <%= f.input :description %>
        <%= f.association :ingredient %>
        <%= f.submit class: "btn btn-primary"%>
        <% end %>
    </div>
</div>

